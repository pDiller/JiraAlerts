version: 2
jobs:
  test:
    docker:
      - image: circleci/openjdk:8-jdk-browsers
    steps:
      - checkout
      - run:
        name: test
        command: ./gradlew check
workflows:
  version: 2
  test_jiraalerts:
    jobs:
      - test