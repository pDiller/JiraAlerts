package io.reflectoring.jiraalerts.dashboard.device;

import org.apache.wicket.markup.html.panel.GenericPanel;

/**
 * Displays the details of a device
 */
public class DeviceDetailsPanel extends GenericPanel<DeviceDTO> {

	public DeviceDetailsPanel(String id) {
		super(id);
	}
}
